<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Respond to Requests</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      color: #fff;
      background: url('https://png.pngtree.com/background/20210711/original/pngtree-geometric-gradient-creative-blood-donation-poster-background-material-picture-image_1127152.jpg') no-repeat center center fixed;
      background-size: cover;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    h1 {
      font-size: 2.5em;
      margin-bottom: 20px;
      text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.5);
      color: #e63946;
    }

    .request-container {
      background-color: rgba(0, 0, 0, 0.6);
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
      width: 100%;
      max-width: 600px;
      margin-bottom: 20px;
    }

    .request-container p {
      margin: 10px 0;
    }

    .request-container button[type="submit"] {
      padding: 10px;
      background-color: #e63946;
      color: white;
      border: none;
      border-radius: 5px;
      font-size: 16px;
      cursor: pointer;
      margin-top: 10px;
    }

    .request-container button[type="submit"]:hover {
      background-color: #d62828;
    }

    .no-requests {
      background-color: rgba(0, 0, 0, 0.6);
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
      width: 100%;
      max-width: 600px;
      text-align: center;
    }
    @media (max-width: 600px) {
      h1 {
        font-size: 2em;
      }

      .request-container {
        padding: 15px;
      }

      .request-container button[type="submit"] {
        font-size: 14px;
        padding: 8px;
      }
    }
  </style>
</head>
<body>
  <h1>Respond to Requests</h1>
  <% if (requests.length > 0) { %>
    <% requests.forEach(request => { %>
      <div class="request-container">
        <p>Request for Blood Group: <%= request.bloodGroup %></p>
        <p>Message: <%= request.message %></p>
        <form action="/respond/<%= request.id %>" method="POST">
          <button type="submit">Accept</button>
        </form>
      </div>
    <% }) %>
  <% } else { %>
    <div class="no-requests">
      <p>No requests available.</p>
    </div>
  <% } %>
</body>
</html>
