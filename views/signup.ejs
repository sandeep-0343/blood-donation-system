<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign Up</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      color: #fff;
      background: url('https://png.pngtree.com/background/20210711/original/pngtree-geometric-gradient-creative-blood-donation-poster-background-material-picture-image_1127152.jpg') no-repeat center center fixed;
      background-size: cover;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    h1 {
      font-size: 2.5em;
      margin-bottom: 20px;
      text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.5);
      color: #e63946;
    }

    form {
      background-color: rgba(0, 0, 0, 0.6);
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
      width: 100%;
      max-width: 400px;
    }

    form input[type="email"],
    form input[type="password"],
    form input[type="text"] {
      width: 100%;
      padding: 12px 0px;
      margin: 10px 14px;
      border: 1px solid #ddd;
      border-radius: 5px;
      font-size: 16px;
    }

    form button[type="submit"] {
      width: 100%;
      padding: 12px;
      background-color: #e63946;
      color: white;
      border: none;
      border-radius: 5px;
      font-size: 16px;
      cursor: pointer;
      margin-top: 10px;
    }

    form button[type="submit"]:hover {
      background-color: #d62828;
    }

    .error {
      color: #ff0000;
      font-size: 14px;
      margin: 10px 0;
      text-align: center;
    }

    a {
      display: block;
      text-align: center;
      margin-top: 15px;
      color: #fff;
      text-decoration: none;
      font-size: 1em;
    }

    a:hover {
      text-decoration: underline;
    }
    @media (max-width: 600px) {
      h1 {
        font-size: 2em;
      }

      form {
        padding: 15px;
      }

      form input[type="email"],
      form input[type="password"],
      form input[type="text"] {
        font-size: 14px;
      }

      form button[type="submit"] {
        font-size: 14px;
        padding: 10px;
      }

      a {
        font-size: 0.9em;
      }
    }
  </style>
</head>
<body>
  <h1>Sign Up</h1>
  <form action="/signup" method="POST">
    <input type="email" name="email" placeholder="Email" required><br>
    <input type="password" name="password" placeholder="Password" required><br>
    <input type="text" name="bloodGroup" placeholder="Blood Group" required><br>
    <button type="submit">Sign Up</button>
  </form>

  <% if (error) { %>
    <p class="error"><%= error %></p>
  <% } %>

  <a href="/login">Already have an account? Login</a>
</body>
</html>
